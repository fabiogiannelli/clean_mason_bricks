# feature_scaffold

Generate a clean architecture feature folder structure across `domain`, `data`, and `presentation`.

## Variables

- `feature_name` (string): feature name in `snake_case` (example: `user_profile`)

## Usage

```bash
mason make feature_scaffold --feature_name user_profile
